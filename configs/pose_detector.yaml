# ─────────────────────────────────────────────
# PoseDetector config
# ─────────────────────────────────────────────
weights: "C:/Users/Admin/ket-qua-train-mixed-v1/weights/best.pt"

conf_threshold: 0.25
iou_threshold: 0.45
imgsz: 640

# Sub-pixel refinement
use_subpixel: true

# Occlusion: góc có confidence < 0.25 sẽ bị coi là không tin cậy
occlusion_min_conf: 0.25
